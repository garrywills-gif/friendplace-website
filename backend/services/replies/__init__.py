"""Inbound replies service (iter160b).

Tracks manually-logged replies from prospects/founding members/outreach
targets. Populates the ``inbound_replies`` collection used by
`services/crm/status.py` and by the Replies UI.
"""
